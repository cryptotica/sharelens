# Forge P1 handoff — approved implementation, not spending
State: prepared
Classification: build_code; risk high; owner Forge; reviewer Hermy.
Pinned worker: OpenCode openai/gpt-6-astra (OpenAI OAuth); never change models or delegate.
User approved P1 and redesign inspired by attached retro pixel-art game interface. Implement synchronously in this run. Do not return a plan, spawn agents, commit, push, deploy, read secrets, sign transactions or use money. Edit only this ShareLens project directory.

## Inputs / startup
Read BRAIN.md, specs/source-original.md (full user scope), specs/sharelens-build-spec.md (corrections), latest run_records, existing code/tests. Root AGENTS and development/coding standard apply. Main knowledge lookup for ShareLens/Slipstream had no hits; use primary Aerodrome/Base docs for missing contract details. Forge role is careful minimal coding; Ponytail full, reuse current viem and math helpers.

## Deliverable
Implement P1 wallet connect (Base account SDK or injected EIP1193 wallet; choose smallest safe solution), disconnect/account/chain events, Base network switch and live raw + scaledBalanceOf balances for selected asset, USDC balance. User-controlled exact-input USDC -> stock buy panel with default 0.50% editable validated slippage, actual executable Aerodrome quote (NOT spot estimate), exact approval then swap with receipt/tx link, quote expiration and transaction simulation. No mainnet sends during build/tests.
Research current official Slipstream router/quoter/factory deployment and ABI, verify onchain pool factory/getPool match plus token order/tickSpacing. Never guess addresses/ABI or use 0x/1inch. Persist source URLs and actual read-only verification output. If verified quote/swap support cannot be established, ship wallet/balance and explicitly disabled buy with exact blocker, not fake enabled buttons. Spec accepts explicitly explained unavailable swap.
Trading fail closed: US or unknown geo, no trusted geo source, stale/paused/invalid oracle, premium >1%, missing pool, wrong chain/account, expired quote, errors, invalid input. Geo is coarse risk signal not compliance: use trusted host headers server-side only when provenance can be trusted, otherwise unknown; do not trust user country query/localStorage/header on arbitrary host. No self-certified bypass. Revalidate fresh oracle/pool/geo/account/chain/quote before each signing boundary, especially AFTER approve. Use bigint for amount/minOut, finite bounded slippage, limited exact allowance, no unlimited approval, reject stale account/asset/amount races. Receipt failure/rejection surfaced, never mark success on hash alone. Builder code attribution only if genuinely configured; never invent a code or silently send unattributed transactions if required. Preserve read-only UX when execution blocked.

## Design direction — required visible redesign, not minor recolor
Reference image: ScorchedTanks Weaponslink pixel-art game shop: layered beveled lavender/gray shell; blue title bar; selected inventory rows; dark inset numeric dashboard; buy/sell action column; chunky tactile buttons, tiny power LED. Translate into tasteful Base BLUE retro financial console. Deep navy canvas, electric blue primary accents, pale cool-blue/lavender panels, near-black inset numerical readouts, restrained cyan and amber statuses. Crisp stepped corners, subtle pixel decorative icon/scan-grid, blue selected stock tabs, compact toolbar/status strip, instrument-like Dividend Lens / Premium Guard and Buy panel. Keep financial numbers large tabular readable sans/mono, NEVER tiny bitmap body copy. No candles, invented charts/yields/balances, generic SaaS/sidebar clutter, game weapon imagery or copy. Layout desktop 2-column main instruments plus coherent buy station, mobile 390px stacked with no horizontal overflow. Distinct header/logo, tactile click states, focus rings, accessible labels/live status, reduced-motion. Preserve disclaimer, source labels and freshness explanation. Use CSS/inline original geometric SVG not copyrighted attached art. Image reference itself need not be embedded.

## Verification / evidence
Run npm install --no-audit --no-fund, npm test, npm run build, npm run check:live. Add focused deterministic wallet/transaction/geo safety tests (mock providers explicitly test-only): US/unknown/stale/paused/premium thresholds, invalid slippage/amount, expiry/context changes, rejection, revalidation after approval. Do not weaken P0 tests. Record observed commands/exit codes, new files, actual route/session metadata if available, exact blockers, source URLs in run_records/p1-forge-observed.md. Do not claim mainnet swap verified without receipt (none authorized). Do not mark responsive/visual verification pass based on CSS alone; Hermy performs browser check.
Recovery: max 2 implementation attempts / 60 min; report exact blocker with artifacts preserved. End with implemented scope, skipped scope and commands/exit codes.
